# Python dependencies for the ADV targeting pipeline.
#
# Versions are the ones this pipeline has actually been run against, not the
# newest available. The output of this pipeline is a sales target list that
# people act on; "it worked last quarter" is worth more here than a point
# release. Bump deliberately, then re-run `python src/audit.py` -- several of
# those checks are recomputations of numbers that a pandas upgrade could move.
#
# Install:  pip install -r requirements.txt

# --- core: everything in the firm/advisor pipeline needs these -------------
pandas==1.2.2
numpy==1.24.3
pyarrow==17.0.0          # Parquet cache in data/interim

# --- reading the SEC feeds -------------------------------------------------
lxml==6.0.2              # the ADV XML feeds are large; the C parser matters
openpyxl==3.1.2          # Barron's / Forbes ranking workbooks
PyYAML==6.0              # mapping.yaml

# --- roster scraping (firm websites, ranking sources) ----------------------
requests==2.31.0
httpx==0.27.0
aiohttp==3.10.11
aiofiles==24.1.0
beautifulsoup4==4.12.2
playwright==1.48.0       # also needs: playwright install chromium
PyMuPDF==1.23.22         # ADV Part 2B brochure PDFs

# --- progress reporting on the long builds ---------------------------------
tqdm==4.65.0

# The Azure Functions in api/ are JavaScript, not Python -- their dependency
# manifest is api/package.json and Azure restores it at deploy time.
